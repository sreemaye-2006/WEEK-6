# Week 6.3 - Vite Project

This directory contains the project created in Week 6.3 using Vite.

## Setup Instructions

1. Navigate to the `vite-project` directory: `cd vite-project`
2. Install dependencies: `npm install`
3. Run the development server: `npm run dev`
